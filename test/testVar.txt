PROCEDURE p1;
	VAR i,j:INTEGER;
BEGIN
	WHILE j > 1 DO
		x := x+1;
		WRITEINT(x);
		WRITEFLOAT(x);
		WRITECHAR(x);
		WRITESTRING(x);

		h := READINT;
		h := READFLOAT;
		h := READCHAR;
		h := READSTRING;
	END;
END p1